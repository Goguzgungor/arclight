# Arckive (arckive) install — generated by scripts/build-install.sh; do not edit by hand.
# kubectl apply -f https://arckive.org/install.yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: arckive-system
  labels:
    app.kubernetes.io/name: arckive-operator
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: indexers.arckive.org
spec:
  group: arckive.org
  names:
    kind: Indexer
    listKind: IndexerList
    plural: indexers
    singular: indexer
    shortNames: [idx]
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Current
          type: integer
          jsonPath: .status.currentBlock
        - name: Head
          type: integer
          jsonPath: .status.headBlock
        - name: Lag
          type: integer
          jsonPath: .status.lag
      schema:
        openAPIV3Schema:
          type: object
          required: [spec]
          properties:
            spec:
              type: object
              required: [network, storage, contracts]
              properties:
                network:
                  type: object
                  required: [chainId, rpc]
                  properties:
                    chainId:
                      type: integer
                      minimum: 1
                    rpc:
                      type: array
                      minItems: 1
                      items:
                        type: string
                        pattern: '^(https?|wss?)://'
                        description: "http(s):// for polling/reads; ws(s):// for newHeads subscription + reads"
                    announceRpc:
                      type: array
                      items:
                        type: string
                        pattern: '^wss?://'
                        description: "ws endpoints used only to listen for newHeads — never added to the query pool"
                    explorerApi:
                      type: string
                      pattern: '^https?://'
                      description: "Blockscout-style API base (…/api/v2) to auto-fetch verified ABIs; defaults per chainId"
                    finalityTag:
                      type: string
                      enum: [finalized, safe, latest]
                      default: finalized
                storage:
                  type: object
                  required: [mode, external]
                  properties:
                    mode:
                      type: string
                      enum: [External]
                    external:
                      type: object
                      required: [dsnSecretRef]
                      properties:
                        dsnSecretRef:
                          type: object
                          required: [name]
                          properties:
                            name:
                              type: string
                            key:
                              type: string
                              default: url
                contracts:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required: [name, address]
                    properties:
                      name:
                        type: string
                        pattern: "^[a-z][a-z0-9-]{0,29}$"
                      address:
                        type: string
                        pattern: "^0x[0-9a-fA-F]{40}$"
                      abi:
                        type: object
                        description: "ABI source; omit entirely to auto-fetch the verified ABI from the explorer by address"
                        properties:
                          configMapRef:
                            type: object
                            required: [name]
                            properties:
                              name:
                                type: string
                              key:
                                type: string
                                default: abi.json
                          inline:
                            type: array
                            description: "inline ABI (array of ABI entries)"
                            items:
                              type: object
                              x-kubernetes-preserve-unknown-fields: true
                      startBlock:
                        type: integer
                        description: "omit = tail from head; negative = last |n| blocks (head-relative); >=0 = absolute block"
                      events:
                        type: array
                        items:
                          type: string
                polling:
                  type: object
                  properties:
                    batchBlocks:
                      type: integer
                      minimum: 1
                      default: 1000
                    intervalMs:
                      type: integer
                      minimum: 1
                      default: 2000
            status:
              type: object
              properties:
                phase:
                  type: string
                  enum: [Provisioning, Backfilling, Live, Degraded]
                currentBlock:
                  type: integer
                headBlock:
                  type: integer
                lag:
                  type: integer
                lastError:
                  type: string
                observedGeneration:
                  type: integer
                conditions:
                  type: array
                  items:
                    type: object
                    required: [type, status, reason, lastTransitionTime]
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      reason:
                        type: string
                      message:
                        type: string
                      lastTransitionTime:
                        type: string
---
# Source: arckive/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: arckive-operator
  namespace: arckive-system
  labels:
    app.kubernetes.io/name: arckive-operator
    app.kubernetes.io/instance: arckive
    app.kubernetes.io/managed-by: Helm
---
# Source: arckive/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: arckive-operator
  labels:
    app.kubernetes.io/name: arckive-operator
    app.kubernetes.io/instance: arckive
    app.kubernetes.io/managed-by: Helm
rules:
  - apiGroups: [arckive.org]
    resources: [indexers]
    verbs: [get, list, watch]
  - apiGroups: [arckive.org]
    resources: [indexers/status]
    verbs: [patch]
  - apiGroups: [apps]
    resources: [deployments]
    verbs: [get, list, watch, create, patch]
  - apiGroups: [""]
    resources: [configmaps, serviceaccounts]
    verbs: [get, list, watch, create, patch]
  - apiGroups: [""]
    resources: [secrets]
    verbs: [get]
  - apiGroups: [rbac.authorization.k8s.io]
    resources: [roles, rolebindings]
    verbs: [get, list, watch, create, patch]
---
# Source: arckive/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: arckive-operator
  labels:
    app.kubernetes.io/name: arckive-operator
    app.kubernetes.io/instance: arckive
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: arckive-operator
subjects:
  - kind: ServiceAccount
    name: arckive-operator
    namespace: arckive-system
---
# Source: arckive/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: arckive-operator
  namespace: arckive-system
  labels:
    app.kubernetes.io/name: arckive-operator
    app.kubernetes.io/instance: arckive
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: arckive-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: arckive-operator
        app.kubernetes.io/instance: arckive
        app.kubernetes.io/managed-by: Helm
    spec:
      serviceAccountName: arckive-operator
      containers:
        - name: operator
          image: "ghcr.io/goguzgungor/arckive-operator:latest"
          imagePullPolicy: Always
          env:
            - name: WORKER_IMAGE
              value: "ghcr.io/goguzgungor/arckive-worker:latest"
            - name: LOG_LEVEL
              value: "info"
            - name: RESYNC_INTERVAL_MS
              value: "300000"
          ports:
            - containerPort: 8080
              name: health
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 128Mi
