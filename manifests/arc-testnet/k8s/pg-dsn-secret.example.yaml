# This is an example: fill in your real DSN and apply it WITHOUT committing the file,
# or run: kubectl create secret generic pg-dsn --from-literal=url='postgres://...'
apiVersion: v1
kind: Secret
metadata:
  name: pg-dsn
stringData:
  url: postgres://USER:PASSWORD@HOST:5432/DATABASE
